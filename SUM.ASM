DATA SEGMENT
  BUF DB 1,2,3,4
  NN EQU $-BUF
  RES DB ?
DATA ENDS

CODE SEGMENT
  ASSUME DS:DATA,CS:CODE
START: MOV AX,DATA
       MOV DS,AX
       LEA BX,BUF
       MOV CX,NN
       MOV AL,0
LP:    ADD AL,[BX]
       INC BX
       LOOP LP
MOV AH,4CH
INT 21H
CODE ENDS
END START
