DATA SEGMENT
  STR DB 'ABCA'
  NN EQU $-STR
  RES DB ?
DATA ENDS

CODE SEGMENT
  ASSUME DS:DATA,CS:CODE
START: MOV AX,DATA
       MOV DS,AX
      LEA BX,STR
      MOV CX,NN
      MOV DL,0
LP:   MOV AL,[BX]
      CMP AL,41H
      JNZ NEXT
      INC DL
NEXT: INC BX
LOOP LP
MOV RES,DL
MOV AH,4CH
INT 21H
CODE ENDS
END START
